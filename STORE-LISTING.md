# Chrome Web Store 上架材料

> 提审时按此文档填写商店后台；隐私政策模板可直接托管到 GitHub Pages 后填入 URL。

## 一、名称（限制 75 字符内）

```
WebMold - 说一句, 网页听你的
```

（字符数：24，符合要求。与 `manifest.json` 的 `name` 保持一致。）

## 二、简介 / 短描述（限制 132 字符内）

```
用一句话定制任意网站：内置 AI Agent 自动探测页面、生成并注入 CSS/JS，规则按域名持久化。
```

（中文字符计 1 字符，共约 47 字符，符合要求。）

## 三、详细描述（限制 16000 字符内，直接复制）

```
WebMold —— 说一句，网页听你的

用自然语言描述你想对任意网站做的改动，内置的 AI Agent 会直接在真实页面上"边看边改"，把需求翻译成可执行的 CSS 和 JS 并注入。不用写代码，就能给网站加功能、改外观。

能做什么：
- 说"隐藏顶部广告"，Agent 先探测页面 DOM、试跑验证，再产出规则；
- 说"给这个站加个夜间模式按钮"，它直接写出能用的 JS；
- 说"在每个视频卡片上显示时长"，也一样；
- 所有规则按网站域名持久化保存，下次访问自动生效。

核心特性：
- Agent 驱动：不是一次性猜代码，而是循环式"探测-试跑-验证"，复杂网站成功率更高；
- 功能 + UI 双定制：CSS 管外观，JS 管功能；
- 按域名持久化：支持整站 / URL 前缀 / 精确 URL 三种生效范围；
- 即时预览：保存前先预览效果，满意再落库；
- 规则管理：启用 / 停用 / 编辑 / 删除 / 导入导出；
- 模型自选：兼容 OpenAI Chat Completions 协议，内置腾讯云 Token Plan 模型清单（GLM / Kimi / DeepSeek / MiniMax 等），也支持接入任何自建服务；
- 过程可视：侧边栏实时展示 Agent 每一步的思考与工具调用。

隐私与安全：
- 默认纯本地存储，无遥测，不上传任何数据；
- API Key 只保存在浏览器本地（chrome.storage.local）；
- Agent 的探测工具只读，试跑脚本带 8 秒超时；
- 生成的 JS 会在页面主世界执行（等同第三方脚本），保存前请务必预览检查。

使用方法：
1. 点击工具栏 WebMold 图标进入设置页，选择模型厂商并填入自己的 API Key；
2. 打开任意网站，点击图标打开侧边栏；
3. 在输入框描述需求 → 点"生成定制"，观看 Agent 实时工作；
4. 预览效果，选择生效范围 → 保存规则，之后每次访问自动应用。

注意：生成结果由 AI 完成，复杂网站可能出现不完美效果，请保存前预览检查。
```

## 四、隐私政策模板

> 将下方内容托管到任意可访问的网页（建议 GitHub Pages / 个人站点），把 URL 填入商店后台。

```
# WebMold 隐私政策

最后更新日期：2026-08-11

## 1. 开发者收集的数据

本扩展的开发者不收集、不存储、不传输任何用户数据。本扩展没有任何服务器端、没有账号系统、没有遥测统计。

## 2. 数据存储

- 所有规则、配置、历史记录均存储在用户浏览器本地的 chrome.storage.local 中。
- 用户填写的 API Key / Token 仅保存在浏览器本地，开发者无法访问。
- 卸载扩展后，浏览器会在符合 Chrome 清理规则时移除相关本地数据。

## 3. 数据的处理与传输

当用户主动使用"生成定制"功能时，扩展会把当前页面的标题、DOM 结构摘要以及用户输入的需求，发送到用户在设置页自行配置并授权的第三方 AI 模型服务（OpenAI 兼容接口），以生成 CSS/JS 规则。开发者不参与也不存储这些请求内容。

## 4. 第三方服务

本扩展依赖用户自行配置的第三方 AI 模型服务。该服务的数据处理行为由其自身隐私政策约束，与开发者无关。

## 5. 用户控制

- 用户可随时在设置页删除、修改或导出自己的规则与配置。
- 用户可随时卸载本扩展，停止一切数据处理。

## 6. 联系方式

如有隐私相关问题，可通过 GitHub 仓库 issue 联系开发者。

---

本隐私政策随扩展版本更新，如内容变更将在此页面说明。
```

## 五、商店问卷勾选建议

| 问卷项 | 建议勾选 |
| --- | --- |
| 是否收集个人身份信息 | 否（开发者不收集） |
| 是否使用认证信息（API Key） | 是，但仅存本地，用于用户自配的模型服务 |
| 是否收集页面内容 / 网页活动 | 用户触发时临时处理（仅用于生成规则），不存储不传输给开发者 |
| 数据处理位置 | 用户配置的第三方模型服务（开发者托管之外） |
| 远程代码 | 否（本扩展不含远程代码） |
| 促销 / 广告 | 否 |
