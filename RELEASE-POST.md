# [分享] WebMold —— 用一句话，让任意网页听你的话

最近做了一个 Chrome 插件叫 **WebMold**，核心思路很简单：**用自然语言描述你想对任意网站做的改动，插件内置的 Agent 会直接在真实页面上"边看边改"，把需求翻译成可执行的 CSS/JS 并注入。**

## 它能做什么

- 对任意网站说"隐藏顶部广告"，它先探测 DOM、试跑验证，再产出规则；
- 说"给这个站加个夜间模式按钮"，它直接写出能用的 JS；
- 说"在每个视频卡片上显示时长"，也一样。

本质是一个**带工具的 Agent**：探测页面 → 试跑 JS → 产出规则 → 按域名持久化，下次访问自动生效。过程在侧边栏实时可视化，你可以看到它每一步在想什么、做了什么。

## 特性

- **Agent 驱动**：不是一次性猜代码，而是循环式"探测-试跑-验证"，复杂网站成功率更高；
- **功能 + UI 双定制**：CSS 管外观，JS 管功能；
- **按域名持久化**：规则以域名为维度保存，支持整站 / URL 前缀 / 精确 URL 三种生效范围；
- **即时预览**：保存前先预览效果，满意再落库；
- **规则管理**：启用 / 停用 / 编辑 / 删除 / 导入导出；
- **模型自选**：OpenAI 兼容协议，内置腾讯云 Token Plan 模型清单（GLM / Kimi / DeepSeek / MiniMax / DeepSeek 原厂直供等），也支持接入任何自建服务；
- **本地 AI 模式**：可选 Chrome 内置 Gemini Nano，全程离线、无需自配 API Key。

## 隐私与安全

- 默认**纯本地存储**，无遥测，不上传任何数据；
- API Key 只存在浏览器本地；
- Agent 的探测工具只读，`try_run_js` 带 8 秒超时；
- 生成的 JS 会在页面主世界执行（等同第三方脚本），**保存前请务必预览检查**——这点已写进默认提示。

## 安装

商店版本正在准备中，目前提供两种安装方式（均需在 `chrome://extensions` 开启「开发者模式」）：

1. **解压安装包**：下载 `webmold.zip` 解压后，「加载已解压的扩展程序」选择该文件夹；
2. **CRX 安装包**：下载 `webmold.crx`，直接拖入扩展页面即可。

使用前在设置页二选一配置模型：

- **云端 API**：选择厂商后填入自己的 API Key / Token（如腾讯云 Token Plan）；
- **本地 Chrome AI**：无需 Key，点「检测本地 AI」确认可用即可（需 Chrome 138+ 及满足硬件要求）。

## 一些说明

- 扩展名：WebMold（web mold，网页"模具"——你想让网页变成什么样，就给什么样）；
- 这是个人独立开发，功能在持续迭代中；
- 如果你愿意，可以帮我验证一下在常用网站上的生成效果，有翻车案例欢迎反馈。

欢迎试用和提意见。
